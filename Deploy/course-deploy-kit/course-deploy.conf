# E-mail alerts for scheduled publications and failures (optional).
# Yandex: SMTP_HOST="smtp.yandex.ru", SMTP_PORT="465", SMTP_USER = full address,
# SMTP_PASSWORD = an app password (not the account password).
SMTP_HOST=""
SMTP_PORT="465"
SMTP_USER=""
SMTP_PASSWORD=""
MAIL_TO=""

# Advanced, defaults shown:
# KEEP_RELEASES="5"
# RETRY_AFTER="900"
# TIMEZONE="Europe/Moscow"
# DATA_DIR="/srv/course-deploy"
