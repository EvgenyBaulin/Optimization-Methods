# The site published by course-deploy: name, web root, URL; '#' starts a comment.
# The name is the first field of Deploy/publish.conf, which reads this file for it.
course  /var/www/optimization-methods  https://optimization-methods.tarakan-tuc.ru
